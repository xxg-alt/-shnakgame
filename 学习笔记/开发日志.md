# 开发日志

## 2026-07-16

### 今日目标
完善游戏主循环，让蛇能真正动起来。

### 完成内容
- [x] 创建 Obsidian 笔记体系
- [x] 梳理开发计划
- [x] 整理控制台绘图原理笔记
- [x] 整理蛇的移动逻辑笔记

### 未完成
- [ ] Update() 方法还是空的，需要实现移动逻辑
- [ ] 游戏循环中缺少 Update() 调用
- [ ] 没有食物生成和碰撞检测

### 明日计划
- [ ] 实现 `Update()` 方法
- [ ] 在主循环中添加 `Update()` 调用
- [ ] 添加 `Thread.Sleep(speed)` 控制速度

### 遇到的问题
> `HandleInput()` 里用 `Console.ReadKey(true)` 会阻塞等待按键，
> 需要改成非阻塞方式，否则蛇不会自动移动。

解决方案：使用 `Console.KeyAvailable` 判断是否有按键，没有就跳过。
当前代码已经这样处理了，但主循环需要配合 `Update()` 才能工作。

## 相关笔记
- [[00-项目开发计划]]
